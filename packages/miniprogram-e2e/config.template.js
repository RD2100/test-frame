/**
 * config.js 模板 — 复制到各项目 tests/e2e/config.js 并修改3行
 */
module.exports = {
  // 1. 自动化端口（改端口改这里）
  automatorPort: 9420,
  // 2. 项目绝对路径（改项目改这里）
  projectPath: 'D:/YOUR_PROJECT_PATH',
  // 3. miniprogram子目录（扁平布局填''，嵌套布局填'miniprogram/'）
  miniprogramRoot: 'miniprogram/',
};