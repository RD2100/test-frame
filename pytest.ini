[pytest]
testpaths =
    tests/android/airtest
    tests/h5
pythonpath =
    .
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    smoke: smoke test
    regression: regression test
    compatibility: compatibility test
    slow: slow test
